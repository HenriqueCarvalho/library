<h1>Items</h1>

<p>There are <%= @items.count %> items!</p>

<% @items.each do |item| %>
	<p>
		<%= item.name %>
	</p>
	<p>
		<%=item.item_type.name %> 
	</p>
	<p>
		<%= image_tag(item.image_url) %>
	</p>
	<p>
		<% if !item.completed? %>    
			 Item is completed.
	  	<% end %>
	</p>
<% end %>